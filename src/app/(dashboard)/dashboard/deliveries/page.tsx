import { DashboardView } from "@/components/dashboard/DashboardView";

export const metadata = { title: "Deliveries — Handiman" };

export default function Page() {
  return <DashboardView page="deliveries" />;
}
