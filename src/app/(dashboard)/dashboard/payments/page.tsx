import { DashboardView } from "@/components/dashboard/DashboardView";

export const metadata = { title: "Payments — Handiman" };

export default function Page() {
  return <DashboardView page="payments" />;
}
