import { DashboardView } from "@/components/dashboard/DashboardView";

export const metadata = { title: "Account settings — Handiman" };

export default function Page() {
  return <DashboardView page="settings" />;
}
