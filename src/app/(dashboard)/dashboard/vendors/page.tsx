import { DashboardView } from "@/components/dashboard/DashboardView";

export const metadata = { title: "Saved vendors — Handiman" };

export default function Page() {
  return <DashboardView page="vendors" />;
}
