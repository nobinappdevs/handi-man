import { PagePlaceholder } from "@/components/share/PagePlaceholder";

export const metadata = { title: "FAQ — Handiman" };

export default function Page() {
  return <PagePlaceholder title="FAQ" />;
}
