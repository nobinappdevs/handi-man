import { PagePlaceholder } from "@/components/share/PagePlaceholder";

export const metadata = { title: "Contact — Handiman" };

export default function Page() {
  return <PagePlaceholder title="Contact" />;
}
