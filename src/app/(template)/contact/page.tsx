import { ContactScreen } from "@/components/contact/ContactScreen";

export const metadata = {
  title: "Contact — Handiman",
  description:
    "Tell us what you need and we will match a verified vendor. Reach the Handiman team by form, email or phone — replies within 2 hours, 24/7.",
};

export default function Page() {
  return <ContactScreen />;
}
