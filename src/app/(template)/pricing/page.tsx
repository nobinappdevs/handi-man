import { PagePlaceholder } from "@/components/share/PagePlaceholder";

export const metadata = { title: "Pricing — Handiman" };

export default function Page() {
  return <PagePlaceholder title="Pricing" />;
}
