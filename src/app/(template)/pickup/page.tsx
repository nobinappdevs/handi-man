import { PagePlaceholder } from "@/components/share/PagePlaceholder";

export const metadata = { title: "Pickup — Handiman" };

export default function Page() {
  return <PagePlaceholder title="Pickup" />;
}
