import { PickupScreen } from "@/components/pickup/PickupScreen";

export const metadata = {
  title: "Pickup — Handiman",
  description:
    "Book a door pickup: list what you are sending, a verified Handiman rider collects it and takes it on — tracked from your door onward.",
};

export default function Page() {
  return <PickupScreen />;
}
