import { BlogScreen } from "@/components/blog/BlogScreen";

export const metadata = {
  title: "Blog — Handiman",
  description:
    "News and announcements from Handiman — how the platform works, what it costs, and what is coming next.",
};

export default function Page() {
  return <BlogScreen />;
}
