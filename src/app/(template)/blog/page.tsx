import { PagePlaceholder } from "@/components/share/PagePlaceholder";

export const metadata = { title: "Blog — Handiman" };

export default function Page() {
  return <PagePlaceholder title="Blog" />;
}
